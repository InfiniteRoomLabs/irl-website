---
import type { WithContext, Organization } from 'schema-dts';

const schema: WithContext<Organization> = {
    '@context': 'https://schema.org',
    '@type': 'Organization',
    name: 'Infinite Room Labs',
    url: 'https://infiniteroomlabs.com',
    description: 'Solo technical consultancy. Legacy modernization, CI/CD rescue, infrastructure as code, MSP integration, agentic AI infrastructure.',
    founder: { '@type': 'Person', name: 'Wes Gilleland' },
    address: {
        '@type': 'PostalAddress',
        addressLocality: 'Lexington',
        addressRegion: 'KY',
        addressCountry: 'US',
    },
    areaServed: { '@type': 'State', name: 'Kentucky' },
};
---
<script type="application/ld+json" set:html={JSON.stringify(schema)} />
